\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[margin=0.75in]{geometry}
\usepackage{enumitem}
\usepackage{titlesec}

{# Section formatting #}
\titleformat{\section}{\large\bfseries}{}{0em}{}[\titlerule]

\begin{document}

{# Header #}
\begin{center}
    {\Huge \textbf{ {{ personal_info.name|default('Your Name') }} }} \\
    \vspace{2pt}
    {{ personal_info.email|default('your.email@example.com') }} $|$ {{ personal_info.phone|default('(555) 123-4567') }} $|$ {{ personal_info.location|default('City, State') }} \\
\end{center}

{# Professional Summary #}
{% if personal_info.summary %}
\section{Professional Summary}
{{ personal_info.summary }}
{% endif %}

{# Work Experience #}
{% if work_experience and work_experience|length > 0 %}
\section{Work Experience}
\begin{itemize}[leftmargin=0.15in, label={}]
{% for exp in work_experience %}
\item
\begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
  \textbf{ {{ exp.company|default('Company') }} } & {{ exp.start_date|default('Start Date') }} - {% if exp.is_current %}Present{% else %}{{ exp.end_date|default('End Date') }}{% endif %} \\
  \textit{\small {{ exp.job_title|default('Job Title') }} } & \textit{\small {{ exp.location|default('Location') }} } \\
\end{tabular*}\vspace{-7pt}
{% if exp.description %}
\item\small{ {{ exp.description }} }
{% endif %}
{% endfor %}
\end{itemize}
{% endif %}

{# Skills #}
{% if skills and skills|length > 0 %}
\section{Skills}
{% for skill in skills %}
{{ skill }}{% if not loop.last %}, {% endif %}
{% endfor %}
{% endif %}

{# Certifications #}
{% if certifications and certifications|length > 0 %}
\section{Certifications}
\begin{itemize}[leftmargin=0.15in, label={}]
{% for cert in certifications %}
\item
\begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
  \textbf{ {{ cert.name|default('Certification') }} } & {{ cert.issue_date|default('Issue Date') }} \\
  \textit{\small {{ cert.issuer|default('Issuer') }} } & {% if cert.expiry_date %}{{ cert.expiry_date }}{% else %}No Expiry{% endif %} \\
\end{tabular*}\vspace{-7pt}
{% endfor %}
\end{itemize}
{% endif %}

{# Publications #}
{% if publications and publications|length > 0 %}
\section{Publications}
\begin{itemize}[leftmargin=0.15in, label={}]
{% for pub in publications %}
\item
\begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
  \textbf{ {{ pub.title|default('Publication Title') }} } & {{ pub.publication_date|default('Publication Date') }} \\
  \textit{\small {{ pub.publisher|default('Publisher') }} } & {% if pub.url %}\href{ {{ pub.url }} }{Link}{% endif %} \\
\end{tabular*}\vspace{-7pt}
{% endfor %}
\end{itemize}
{% endif %}

\end{document}
